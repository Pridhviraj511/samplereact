def var=readJSON file: './var.json'
pipeline {
    agent any

    stages {
        stage('Read JSON file') {
            steps {
                script {
                    sh 'echo "My name is ${var.NAME}"'
                }
            }
        }
    }
}
